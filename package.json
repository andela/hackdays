{
  "name": "hackdays",
  "version": "1.0.0",
  "description": "Hackdays@Andela",
  "main": "index.js",
  "repository": "https://github.com/andela/hackdays.git",
  "author": "Paul Dariye <paul@andela.com>",
  "license": "UNLICENCED",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development && yarn run clean && parcel serve -p 1234 ./src/index.html",
    "build": "yarn run clean && NODE_ENV=production build ./src/index.html --no-cache --no-source-maps --public-url",
    "deploy": "yarn run clean && yarn run build && github-pages ",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "gh-pages": "^1.2.0",
    "node-sass": "^4.9.2",
    "parcel-bundler": "^1.9.7",
    "rimraf": "^2.6.2"
  }
}
